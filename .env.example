# =============================================================================
# HOMELAB CREDENTIALS TEMPLATE
# Copy to .env (gitignored) and fill in real values from /etc/homelab/secrets/
# on the geek host (sudo required).
# =============================================================================

# --- Host / Global ---
HOSTNAME=geek
PUBLIC_DOMAIN=your-public-domain.com
INTERNAL_DOMAIN=geek

# --- PostgreSQL (shared geek-postgres) ---
POSTGRES_PASSWORD=***

# --- Authentik SSO ---
# https://auth.geek / https://auth.PUBLIC_DOMAIN
# Admin UI user: akadmin
AUTHENTIK_SECRET_KEY=***
AUTHENTIK_POSTGRESQL__PASSWORD=***
AUTHENTIK_TOKEN=***
AUTHENTIK_OUTPOST_TOKEN=***

# --- BookStack (wiki) ---
# http://bookstack.geek / https://bookstack.PUBLIC_DOMAIN
BOOKSTACK_APP_KEY=base64:***
BOOKSTACK_DB_PASSWORD=***
BOOKSTACK_DB_ROOT_PASSWORD=***
BOOKSTACK_API_TOKEN_ID=***
BOOKSTACK_API_TOKEN_SECRET=***

# --- Forgejo (git hosting) ---
# http://forgejo.geek  |  SSH: git@forgejo.geek (port 222)
# Admin user: johnb
FORGEJO_DB_PASSWORD=***
FORGEJO_SECRET_KEY=***
FORGEJO_INTERNAL_TOKEN=***
FORGEJO_API_TOKEN=***

# --- Woodpecker CI ---
# http://woodpecker.geek  |  OAuth via Forgejo
WOODPECKER_AGENT_SECRET=***
WOODPECKER_FORGEJO_CLIENT=***
WOODPECKER_FORGEJO_SECRET=***
WOODPECKER_DATABASE_DATASOURCE=postgres://woodpecker:***@geek-postgres:5432/woodpecker?sslmode=disable

# --- Pi-hole (DNS / ad-blocking) ---
# http://PI_HOLE_IP/admin
PIHOLE_WEBPASSWORD=***

# --- Cloudflare DDNS ---
CLOUDFLARE_API_TOKEN=***
CLOUDFLARE_DOMAINS=PUBLIC_DOMAIN,*.PUBLIC_DOMAIN

# --- Plane (project management) ---
# http://plane.geek  |  Admin: admin@plane.geek
PLANE_API_TOKEN=***
PLANE_SECRET_KEY=***
PLANE_DB_PASSWORD=***
PLANE_INTERNAL_SECRET_KEY=***
PLANE_INTERNAL_POSTGRES_PASSWORD=***

# --- Vaultwarden (password manager) ---
# http://vaultwarden.geek (LAN) | https://vaultwarden.johnnyblabs.com (WAN)
# Bitwarden-compatible — use Bitwarden app/extension, set server URL to WAN address
# DB setup (one-time): docker exec geek-postgres psql -U postgres -c
#   "CREATE USER vaultwarden WITH PASSWORD '***'; CREATE DATABASE vaultwarden OWNER vaultwarden ENCODING 'UTF8';"
# Generate ADMIN_TOKEN: docker run --rm -it vaultwarden/server /vaultwarden hash --preset owasp
VAULTWARDEN_DATABASE_URL=postgresql://vaultwarden:***@geek-postgres:5432/vaultwarden
VAULTWARDEN_ADMIN_TOKEN=argon2id:***
