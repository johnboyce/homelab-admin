# Forgejo secrets — copy to /etc/homelab/secrets/forgejo.env and fill in real values
# Generate SECRET_KEY: openssl rand -hex 32
# Generate INTERNAL_TOKEN: openssl rand -hex 64

FORGEJO__database__PASSWD=changeme_db_password
FORGEJO__security__SECRET_KEY=changeme_64char_hex_secret
FORGEJO__security__INTERNAL_TOKEN=changeme_jwt_internal_token
