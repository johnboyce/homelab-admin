# Plane Configuration
# Copy to /etc/homelab/secrets/plane.env on geek host
# Replace placeholder values with actual secrets

# Plane Secret Key (generate with: python -c "import secrets; print(secrets.token_urlsafe(50))")
PLANE_SECRET_KEY=your-secret-key-here

# Database Password for plane user in PostgreSQL
PLANE_DB_PASSWORD=your-db-password-here

# Email Configuration (optional)
MAIL_FROM_NAME="Plane"
MAIL_FROM=plane@example.com
