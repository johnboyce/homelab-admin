# Woodpecker CI secrets — copy to /etc/homelab/secrets/woodpecker.env and fill in real values
# AGENT_SECRET: shared secret between server and agent — generate with: openssl rand -hex 32
# FORGEJO_CLIENT/SECRET: from Forgejo → User Settings → Applications → OAuth2 Apps
# DATABASE_DATASOURCE: fill in real woodpecker DB password

WOODPECKER_AGENT_SECRET=changeme_shared_secret
WOODPECKER_FORGEJO_CLIENT=changeme_forgejo_oauth_client_id
WOODPECKER_FORGEJO_SECRET=changeme_forgejo_oauth_client_secret
WOODPECKER_DATABASE_DATASOURCE=postgres://woodpecker:changeme_db_password@geek-postgres:5432/woodpecker?sslmode=disable
