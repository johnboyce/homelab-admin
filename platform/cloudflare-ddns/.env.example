# Copy this file to /etc/homelab/secrets/cloudflare-ddns.env on geek
# chmod 600 /etc/homelab/secrets/cloudflare-ddns.env

# Cloudflare API token with Zone / DNS / Edit permission for johnnyblabs.com
# Same token used by acme.sh (CF_Token in ~/.acme.sh/account.conf)
CF_API_TOKEN=your-cloudflare-api-token-here

# Comma-separated list of DNS A records to keep updated with your public IP
DOMAINS=johnnyblabs.com,*.johnnyblabs.com,www.johnnyblabs.com
